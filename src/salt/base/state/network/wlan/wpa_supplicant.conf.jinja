ctrl_interface=DIR=/var/run/wpa_supplicant GROUP=netdev
update_config=1
country={{ salt['pillar.get']('country_code', 'gb') }}
{%- for network in salt['grains.get']('wpa_supplicant:networks', []) %}
network={
{%- for key, val in network.iteritems() %}
    {{ key }}={{ val|tojson|safe }}
{%- endfor %}
}
{%- endfor %}
